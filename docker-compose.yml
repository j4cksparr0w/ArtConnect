services:
  app:
    build: .
    expose:
      - "8501"

  nginx:
    image: nginx:alpine
    ports:
      - "8081:8080"
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
